import {totalRows,totalColumns,colorAt,bounds} from './model.mjs';
export function sizeCanvas(canvas){const {width,height}=canvas.getBoundingClientRect();if(!width||!height)return null;const dpr=Math.min(devicePixelRatio||1,3);if(canvas.width!==Math.round(width*dpr)||canvas.height!==Math.round(height*dpr)){canvas.width=Math.round(width*dpr);canvas.height=Math.round(height*dpr);}const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,width,height);return {ctx,width,height};}
const tiles=new Map();
function yarn(color){if(tiles.has(color))return tiles.get(color);const tile=document.createElement('canvas');tile.width=80;tile.height=100;const c=tile.getContext('2d');c.fillStyle=color;c.fillRect(0,0,80,100);c.fillStyle='#00000023';c.fillRect(0,0,80,100);c.lineCap='round';
 for(const mirror of [false,true]){c.save();if(mirror){c.translate(80,0);c.scale(-1,1);}for(let i=0;i<13;i++){c.beginPath();c.moveTo(12,-12);c.bezierCurveTo(14,20,29,75,39,94);c.lineWidth=30-i*1.6;c.strokeStyle=i===0?'#00000032':color;c.stroke();if(i>0){c.strokeStyle=`rgba(255,255,255,${i*.012})`;c.stroke();}}for(let i=0;i<5;i++){c.beginPath();c.moveTo(6+i*3,-10);c.bezierCurveTo(9+i*3,24,25+i*2,77,36+i,94);c.strokeStyle='#ffffff22';c.lineWidth=.6;c.stroke();}c.restore();}tiles.set(color,tile);if(tiles.size>100)tiles.delete(tiles.keys().next().value);return tile;}
export function draw(canvas,p,view,preview=false,selection=null){const s=sizeCanvas(canvas);if(!s)return;const {ctx:c,width:w,height:h}=s;const cell=view.cell,cy=preview?cell*1.22:cell,gutter=preview?0:42;const columns=totalColumns(p),rows=totalRows(p);view.x=preview&&columns*cell<w?(w-columns*cell)/2:Math.min(gutter,Math.max(Math.min(gutter,w-columns*cell),view.x));view.y=Math.min(0,Math.max(Math.min(0,h-rows*cy),view.y));
 c.fillStyle=preview?'#eaeade':'#f4f0e6';c.fillRect(0,0,w,h);const startX=Math.max(0,Math.floor((gutter-view.x)/cell)),endX=Math.min(columns,Math.ceil((w-view.x)/cell));const startY=Math.max(0,Math.floor(-view.y/cy)),endY=Math.min(rows,Math.ceil((h-view.y)/cy));const palette=new Map(p.yarns.map(y=>[y.id,y.color]));
 c.save();c.beginPath();c.rect(gutter,0,w-gutter,h);c.clip();
 for(let vy=startY;vy<endY;vy++){const r=rows-1-vy;for(let x=startX;x<endX;x++){const px=view.x+x*cell,py=view.y+vy*cy,color=palette.get(colorAt(p,x,r));if(preview)c.drawImage(yarn(color),px,py,cell,cy);else{c.fillStyle=color;c.fillRect(px,py,cell,cy);c.strokeStyle='#776e5a44';c.lineWidth=.5;c.strokeRect(px,py,cell,cy);if(selection){const b=bounds(selection);if(x%p.columns>=b.left&&x%p.columns<=b.right&&r%p.rows>=b.bottom&&r%p.rows<=b.top){c.fillStyle='#52796b44';c.fillRect(px,py,cell,cy);c.strokeStyle='#52796b';c.lineWidth=2;c.strokeRect(px+1,py+1,cell-2,cy-2);}}}}
 if(!preview&&p.showDividers&&r%20===0){c.strokeStyle='#495b50';c.lineWidth=1.7;c.beginPath();c.moveTo(gutter,view.y+(vy+1)*cy);c.lineTo(w,view.y+(vy+1)*cy);c.stroke();}}
 if(!preview){if(p.showDividers)for(let x=20;x<columns;x+=20){const px=view.x+x*cell;if(px>=gutter&&px<=w){c.strokeStyle='#495b50';c.lineWidth=1.7;c.beginPath();c.moveTo(px,0);c.lineTo(px,h);c.stroke();}}const py=view.y+(rows-p.currentRow)*cy;c.strokeStyle='#8d1735';c.lineWidth=2.5;c.strokeRect(gutter,py,w-gutter-1,cy);}
 c.restore();if(!preview){c.fillStyle='#f5f7f2';c.fillRect(0,0,gutter,h);c.font='12px -apple-system, sans-serif';c.textBaseline='middle';for(let vy=startY;vy<endY;vy++){const row=rows-vy,y=view.y+vy*cy;if(row===p.currentRow){c.fillStyle='#8d1735';c.fillRect(0,y,gutter,cy);}c.fillStyle=row===p.currentRow?'white':'#52665a';c.textAlign='right';c.fillText(String(row),gutter-5,y+cy/2);if(p.completedRows.includes(row)){c.fillStyle='#2d8650';c.textAlign='left';c.fillText('✓',2,y+cy/2);}}}
}
